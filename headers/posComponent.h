#include "globalVars.h"

#ifndef POSCOMPONENT_H
#define POSCOMPONENT_H

/*class PosComponent{

    pc* components[MAX_ENTITIES];

public:
    struct pc{
        int x;
        int y;
        pc(int px, int py) : x(px), y(py) {};
    };

    PosComponent();
    ~PosComponent();

    void addComponent(int eid, int px = 0, int py = 0);

    void removeComponent(int eid);

    pc getComponent(int eid);

    void setComponent(int eid, int px, int py);
};
*/

#endif // POSCOMPONENT_H
